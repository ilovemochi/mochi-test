import { palette } from '../../palette';
import { radii } from '../../radii';

export const normal = {
  border: `0.2rem solid ${palette.NEUTRAL_100}`,
  borderRadius: radii.S,
};

export const rounded = {
  border: `0.2rem solid ${palette.NEUTRAL_100}`,
  borderRadius: '50%',
};
